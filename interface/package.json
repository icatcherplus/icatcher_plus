{
  "name": "icatcher-interface",
  "version": "1.0.0",
  "description": "All GUIs for iCatcher+",
  "main": "server.js",
  "scripts": {
    "postinstall": "cd ./frontend/results_tool && npm ci",
    "prestart": "cd ./frontend/results_tool && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
